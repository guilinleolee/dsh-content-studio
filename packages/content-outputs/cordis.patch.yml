# dsh bundle patch: inserts the outputs-library Remote gateway into a
# profile's layer stack (`dsh plugin add` consumes this via dsh.bundle.patch).
- insert:
    - id: content-outputs
      name: '@guilinleolee/dsh-content-outputs'
